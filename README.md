# CRUD-operation_springJPA
Spring Boot REST API with Spring Data JPAThis repository contains a production-ready, backend RESTful API built using Spring Boot and Spring Data JPA. It demonstrates full CRUD (Create, Read, Update, Delete) operations, proper database connectivity, and clean architectural patterns
